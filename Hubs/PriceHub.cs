using Microsoft.AspNetCore.SignalR;

namespace WarehouseApi.Hubs
{
    public class PriceHub : Hub
    {
        // Keep this empty for now. 
        // We just need the class to exist so we can map it in Program.cs
    }
}